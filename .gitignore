node_modules/
.nuxt/
.output/
dist/
.data/
.env
.env.*
!.env.example
.DS_Store
*.log
scratch/

# The onion gateway builds into the shared target dir set by ~/.cargo/config.toml,
# but keep a local one out of the tree too.
onion/target/

# Worktrees created by tooling alongside the repo.
.worktrees/
